# Required for registration and slug lookup
VITE_PACKAGE_ID=
VITE_REGISTRY_ID=

# Temporary S3 upload API (Lambda Function URL). No trailing slash.
VITE_UPLOAD_API_BASE=

# Public media CDN origin (terraform public_url). Default: https://dapp-media.evefrontier.com
# Do not set this to a *.cloudfront.net distribution hostname.
# VITE_MEDIA_CDN_BASE=https://dapp-media.evefrontier.com

# Optional: exact hostname of a legacy CloudFront distribution whose URLs
# should be rewritten to VITE_MEDIA_CDN_BASE on read. Leave unset unless this
# project previously published media directly under a CloudFront domain.
# VITE_LEGACY_CLOUDFRONT_HOST=d111111abcdef8.cloudfront.net

# testnet | mainnet | devnet | localnet (defaults to testnet)
VITE_SUI_NETWORK=testnet

# Optional: prefill a smart assembly object for SmartObjectProvider (assembly-focused views)
# VITE_OBJECT_ID=

# Merge local fixture listings into the catalog. Only the dev environment sets
# this to true; test and live leave it unset so they render chain data only.
# VITE_ENABLE_FIXTURE_DATA=true

# Re-enable the Walrus read/publish path. Off everywhere while S3 is the storage
# provider; the Walrus code is kept for the later mainnet flow.
# VITE_ENABLE_WALRUS=true

# Optional overrides — public testnet/mainnet defaults apply (unused while S3 publish is active)
VITE_WALRUS_AGGREGATOR_URL=
VITE_WALRUS_UPLOAD_RELAY=
