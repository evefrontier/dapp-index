# Public Vite env only (never put secrets here — they ship to the browser).

# Optional: Sui object id for @evefrontier/dapp-kit SmartObjectProvider (assembly flows).
# Leave unset for directory shell without a focused smart object.
# VITE_OBJECT_ID=
